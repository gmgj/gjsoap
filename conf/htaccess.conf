<Directory "F:/bit5411/apps/gjsoap/htdocs/libraries">
# This folder does not require access over HTTP
# (the following directive denies access by default)
Order allow,deny

</Directory>
<Directory "F:/bit5411/apps/gjsoap/htdocs/setup/frames">
# This folder does not require access over HTTP
# (the following directive denies access by default)
Order allow,deny

</Directory>
<Directory "F:/bit5411/apps/gjsoap/htdocs/setup/lib">
# This folder does not require access over HTTP
# (the following directive denies access by default)
Order allow,deny

</Directory>
